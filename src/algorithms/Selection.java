package algorithms;

public class Selection {
	
	public static <T extends Comparable<T>>
	void nthElement(T[] data, int first, int nth, int last)
	{
		while (last - first > 3) {
			// cut = partition
		}
		Sorting.insertionSort(data, first, last);
	}
	
}
